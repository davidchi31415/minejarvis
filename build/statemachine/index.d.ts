import { NestedStateMachine } from 'mineflayer-statemachine';
import { Bot } from 'mineflayer';
declare function createRootLayer(bot: Bot, data: any): NestedStateMachine;
export default createRootLayer;
